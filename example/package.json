{
  "name": "@keenmate/svelte-spa-router-example",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:hash": "cross-env VITE_ROUTING_MODE=hash vite --port 5051",
    "dev:history": "cross-env VITE_ROUTING_MODE=history vite --port 5050",
    "build": "vite build",
    "build:hash": "cross-env VITE_ROUTING_MODE=hash vite build --outDir dist-hash",
    "build:history": "cross-env VITE_ROUTING_MODE=history vite build --outDir dist-history",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "cross-env": "^7.0.3",
    "sass": "^1.93.2",
    "svelte": "^5.0.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@keenmate/svelte-spa-router": "file:.."
  }
}
